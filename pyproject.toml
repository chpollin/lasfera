[tool.poetry]
name = "lasfera-django"
version = "0.1.0"
description = "Web files for the La Sfera project."
authors = ["Jason Heppler <jheppler@gmu.edu>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
django = "^5.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
